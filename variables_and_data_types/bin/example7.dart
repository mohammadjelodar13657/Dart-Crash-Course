void main(List<String> args) {
  const myList = [1, 2, 3];

  // This is not true. const value can not be reassigned
  // myList = [4, 5, 6];

  // We cannot add any element to const list
  // myList.add(5);

  print(myList);
}